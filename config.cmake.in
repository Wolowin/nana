set(@PROJECT_NAME@_INCLUDE_DIRS "@CMAKE_INSTALL_PREFIX@/include")



if(NOT TARGET @PROJECT_NAME@)

    
    add_library(@PROJECT_NAME@ UNKNOWN IMPORTED)


    set_target_properties(@PROJECT_NAME@ PROPERTIES INTERFACE_INCLUDE_DIRECTORIES "@CMAKE_INSTALL_PREFIX@/include")



    file(GLOB FOUND_LIB "@CMAKE_INSTALL_PREFIX@/lib/*nana*")
    set_target_properties(@PROJECT_NAME@ PROPERTIES IMPORTED_LINK_INTERFACE_LANGUAGES "CXX" IMPORTED_LOCATION ${FOUND_LIB})


endif()